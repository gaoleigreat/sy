spring:
  cloud:
    config:
      fail-fast: true
      uri: http://114.116.44.104:48020
      profile: dev
      label: master
      username: admin
      password: admin123
      retry:
        max-attempts: 3
    stream:
      bindings:
        output_log:
          destination: printLog
          content-type: application/json
  security:
    user:
      name: ${define.user.name}
      password: ${define.user.password}