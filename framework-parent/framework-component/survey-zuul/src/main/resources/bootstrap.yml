server:
  port: 50010
  servlet:
    context-path: /survey
spring:
  application:
    name: survey-zuul
  profiles:
    active: dev

session:
  domain: survey-zuul

feign:
  hystrix:
    enabled: true

define:
  zuul:
    gray: false
