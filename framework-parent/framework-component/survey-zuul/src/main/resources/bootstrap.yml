server:
  port: 50010
  servlet:
    context-path: /survey
spring:
  application:
    name: survey-zuul
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB

session:
  domain: survey-zuul

feign:
  hystrix:
    enabled: true

define:
  zuul:
    gray: false
